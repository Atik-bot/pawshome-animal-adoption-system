# This directory is created at runtime by config/db.php for PHP session storage.
# InfinityFree does not allow writing to the default /php_sessions path,
# so sessions are stored here instead.
# Upload this README along with your project files.
